# libmem: memory-management libraries.
#
#   tlsf   - TLSF (Two-Level Segregated Fit) unified allocator
#            (pure core + FreeRTOS integration layer + C++ new/delete hooks)
add_subdirectory(tlsf)
